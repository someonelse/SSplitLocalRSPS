{
  "java.project.sourcePaths": [
    "Client/src",
    "Server/src"
  ],
  "java.project.referencedLibraries": [
    // include every .jar under Server/deps
    "${workspaceFolder}/Server/deps/*.jar",
    // (if you ever add client-side jars)
    "${workspaceFolder}/Client/deps/*.jar"
  ]
}
